PREFIX rcw: <https://cours.iut-orsay.fr/rcw/>

SELECT ?code ?nomDept (MIN(?loyer) AS ?loyerMinAppartDept)
WHERE {
  ?dept rcw:comprend ?ville ;
    rcw:codeDep ?code ;
    rcw:nomDept ?nomDept .
  ?ville rcw:loyerMinAppart2024 ?loyer .
}
GROUP BY ?code ?nomDept