PREFIX rcw: <https://cours.iut-orsay.fr/rcw/>

SELECT ?code ?nomDept (MAX(?loyer) AS ?loyerMaxAppartDept)
WHERE {
  ?dept rcw:comprend ?ville ;
    rcw:codeDep ?code ;
    rcw:nomDept ?nomDept .
  ?ville rcw:loyerMaxAppart2018 ?loyer .
}
GROUP BY ?code ?nomDept