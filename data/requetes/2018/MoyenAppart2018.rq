PREFIX rcw: <https://cours.iut-orsay.fr/rcw/>

SELECT ?code ?nomDept (AVG(?loyer) AS ?loyerMoyenAppartDept)
WHERE {
  ?dept rcw:comprend ?ville ;
    rcw:codeDep ?code ;
    rcw:nomDept ?nomDept .
  ?ville rcw:loyerAppart2018 ?loyer .
}
GROUP BY ?code ?nomDept